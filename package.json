{
  "name": "my-super-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal automations, scheduled jobs and small apps",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "mr-green:calendar": "node --env-file-if-exists=.env.local automations/mr-green-calendar/index.ts",
    "test": "node --test \"automations/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2"
  }
}
